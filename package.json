{
  "name": "food-front",
  "version": "1.0.0",
  "description": "Frontend for our Fast Food App",
  "main": "server.js",
  "scripts": {
    "build": "npm run clean && babel ./ --out-dir dist/ --ignore node_modules,ui",
    "clean": "rm -rf dist",
    "start": "npm run build && node dist/server.js"
  },
  "keywords": [],
  "author": "Eze Daniel",
  "license": "ISC",
  "dependencies": {
    "babel-cli": "^6.26.0",
    "babel-preset-env": "^1.7.0",
    "express": "^4.16.4"
  },
  "devDependencies": {
    "eslint": "^5.6.1",
    "eslint-config-airbnb-base": "^13.1.0",
    "eslint-plugin-import": "^2.14.0"
  }
}
